package main

// // Don't even get me started with pointers.. but he's what take out of it.

// func setTo10(b *int) { // Receives a pointer to an integer
// 	*b = 10        // Sets the value inside that address to 10
// 	fmt.Println(b) // Prints the ADDRESS
// }

func main() {
	// 	a := 20         // Regular variable attribution
	// 	fmt.Println(a)  // Print regular value of variable (20)
	// 	setTo10(&a)     // Passes a as the pointer to setTo10()
	// 	fmt.Println(a)  // Prints the new value of a (10)
	// 	fmt.Println(&a) // Prints address of a (which is the same address as b)

	// i := 42
	// p := &i         //point to i
	// fmt.Println(p)  // prints address of i because of line above
	// fmt.Println(&i) // prints the address of i
	// fmt.Println(&p) // prints address of p
	// fmt.Println(*p) // prints the value of p (which is the value of i)
	// fmt.Println(i)  // prints the value of i

}
